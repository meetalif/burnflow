name: burnflow
description: "A new Flutter project."
publish_to: "none"
version: 0.2.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter_markdown:
  flutter_svg:
  widgets_to_image:
  path_provider:
  htmltopdfwidgets:
  url_launcher:
  markdown:
  printing:
  flutter_html_to_pdf:
  intl:
  waveui:
    path: ../waveui
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
